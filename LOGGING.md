# Система логирования

## Обзор

Проект использует многоуровневую систему логирования с поддержкой:
- 🖥️ **Консольный вывод** с цветовым кодированием
- 📁 **Файловое логирование** с ротацией
- 📱 **Telegram уведомления** для критических ошибок
- 🔧 **Контекстная информация** (chat_id, user_id)
- ⚙️ **Настройка через переменные окружения**

## Переменные окружения

### Основные
- `LOG_LEVEL` - Уровень логирования консоли (DEBUG, INFO, WARNING, ERROR, CRITICAL)
- `API_TOKEN` - Токен бота (обязательно)
- `FOR_LOGS` - Chat ID для отправки ошибок (обязательно)

### Дополнительные
- `LOG_TO_FILE` - Включить файловое логирование (true/false, по умолчанию: true)
- `LOG_TO_TELEGRAM` - Включить Telegram уведомления (true/false, по умолчанию: true)
- `LOG_FILE_LEVEL` - Уровень для файлов (по умолчанию: ERROR)
- `LOG_TELEGRAM_LEVEL` - Уровень для Telegram (по умолчанию: ERROR)

## Примеры конфигурации

### Разработка (максимальная детализация)
```bash
LOG_LEVEL=DEBUG
LOG_TO_FILE=true
LOG_TO_TELEGRAM=false
LOG_FILE_LEVEL=DEBUG
```

### Продакшн (только ошибки)
```bash
LOG_LEVEL=ERROR
LOG_TO_FILE=true
LOG_TO_TELEGRAM=true
LOG_FILE_LEVEL=ERROR
LOG_TELEGRAM_LEVEL=CRITICAL
```

### Отладка (без файлов, только консоль)
```bash
LOG_LEVEL=DEBUG
LOG_TO_FILE=false
LOG_TO_TELEGRAM=false
```

## Использование в коде

### Базовое логирование
```python
from logger import logger

logger.info("Информационное сообщение")
logger.warning("Предупреждение")
logger.error("Ошибка")
logger.critical("Критическая ошибка")
```

### Логирование с контекстом
```python
from logger import log_with_context

# В обработчиках Telegram
context_logger = log_with_context(chat_id=message.chat.id, user_id=message.from_user.id)
context_logger.info("Пользователь выполнил действие")
context_logger.error("Ошибка в обработке команды")
```

### Специализированные логгеры
```python
from logger import get_logger

db_logger = get_logger("bun_bot.database")
api_logger = get_logger("bun_bot.api")

db_logger.info("Подключение к базе данных")
api_logger.error("Ошибка API запроса")
```

## Функции системы

### 🎨 Цветовое кодирование (консоль)
- 🔍 DEBUG: Серый
- ℹ️ INFO: Зеленый  
- ⚠️ WARNING: Желтый
- ❌ ERROR: Красный
- 🚨 CRITICAL: Красный с фоном

### 📁 Файловое логирование
- Путь: `logs/bot.log`
- Ротация: 10 файлов по 10MB
- Кодировка: UTF-8
- По умолчанию: только ERROR и выше

### 📱 Telegram уведомления
- Отправка в указанный chat (FOR_LOGS)
- Защита от спама: не чаще 1 раза в минуту для одного типа ошибки
- Форматированные сообщения с контекстом
- Stack trace для серьезных ошибок
- По умолчанию: только ERROR и CRITICAL

### 🔧 Контекстная информация
Каждое сообщение содержит:
- Временная метка
- Название модуля/логгера  
- Уровень сообщения
- Текст сообщения
- Chat ID (если доступен)
- User ID (если доступен)

## Примеры сообщений

### Консоль
```
2025-09-11 22:45:30 | bun_bot | INFO     | Задача запущена | [chat_id:N/A, user_id:N/A]
2025-09-11 22:45:31 | bun_bot | ERROR    | Ошибка отправки сообщения | [chat_id:123456, user_id:789012]
```

### Telegram уведомление
```
🚨 Bot Error Report

🕐 Time: 2025-09-11 22:45:31
⚠️ Level: ERROR  
📁 Module: handlers.py:45
💬 Message: Ошибка отправки сообщения в чат
💭 Chat ID: 123456
👤 User ID: 789012

📋 Traceback:
ConnectionError: Failed to connect to Telegram API
```

## Внешние библиотеки

Система автоматически настраивает уровень логирования для:
- `aiogram` - Telegram Bot API
- `aiohttp` - HTTP клиент
- `aiosqlite` - База данных
- `aiocron` - Планировщик задач

При `LOG_LEVEL=ERROR` внешние библиотеки переводятся в режим WARNING, чтобы не засорять логи.

## Рекомендации

### Для разработки
```bash
LOG_LEVEL=INFO
LOG_TO_FILE=true  
LOG_TO_TELEGRAM=false
```

### Для продакшна
```bash
LOG_LEVEL=ERROR
LOG_TO_FILE=true
LOG_TO_TELEGRAM=true
LOG_FILE_LEVEL=WARNING
LOG_TELEGRAM_LEVEL=CRITICAL
```

### Для отладки конкретной проблемы
```bash
LOG_LEVEL=DEBUG
LOG_TO_FILE=true
LOG_TO_TELEGRAM=false
LOG_FILE_LEVEL=DEBUG
```